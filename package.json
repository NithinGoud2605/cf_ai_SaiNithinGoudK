{
  "name": "cf_ai_cloudflare_agent_chat",
  "version": "1.0.0",
  "description": "AI-powered chat application built on Cloudflare Workers with Llama 3.3",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "pages:dev": "wrangler pages dev public",
    "pages:deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.19.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.0"
  }
}
